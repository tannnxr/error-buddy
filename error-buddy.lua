--[[

	* Name: Script Checker (Beta)
	* Description: Checks your scripts for errors on run time.  This will help ease debugging.
	* Author: Tanner (@tannnnxr)
	* Version: 1.0.0

]] --




local PLUGIN: Plugin = plugin;
local TOOLBAR: PluginToolbar = PLUGIN:CreateToolbar("Script Checker");
local PLUGIN_TOOLBAR_BUTTON = TOOLBAR:CreateButton("", "Opens the UI to check and monitor script errors", "", "Open UI")


-- Initiate the plugin
do
	local PLUGIN: Plugin = plugin;
	local TOOLBAR: PluginToolbar = PLUGIN:CreateToolbar("Script Checker");
	local PLUGIN_TOOLBAR_BUTTON = TOOLBAR:CreateButton("", "Opens the UI to check and monitor script errors", "", "Open UI")
	
	
end
